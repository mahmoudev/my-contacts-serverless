export interface Contact {
  userId: string
  contactId: string
  createdAt: string
  contactName: string
  contactNumber: string
  contactEmail: string
  contactGroup: string
  contactImageURL?: string
}
